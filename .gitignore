# Afhankelijkheden
node_modules/

# Bouwresultaten
out/
dist/
manager/out/
manager/dist/
*.tsbuildinfo

# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# Verzamelde data en persoonlijke gegevens -- NOOIT in de repo.
#
# data/ bevat de matchdatabase (honderden MB's en groeiend), de crawlstaat, en
# backups van je eigen rune- en masterypagina's met je account-id's erin. Dat
# hoort thuis op de machine van de gebruiker, niet in versiebeheer.
# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
#
# LET OP de schuine streep vooraan. Zonder die streep matcht 'data/' ELKE map
# die zo heet, op elk niveau -- en dat is twee keer misgegaan. Eerst verdween
# site/data/ met de cijfers van de website, later manager/src/main/data/ met
# browser.ts en writeQueue.ts erin. Dat laatste kwam pas aan het licht toen de
# CI de repo van een schone kloon probeerde te typechecken: die twee bestanden
# stonden wel op de machine van de bouwer en nergens anders.
#
# '/data/' is verankerd aan de wortel van de repo en raakt dus alleen de
# matchdatabase, niet een bronmap die toevallig dezelfde naam heeft.
/data/
/server-data/
*.jsonl
*.jsonl.lock
*.jsonl.tmp-*
*.jsonl.stale-*

# Sleutels en instellingen
.env
.env.*
settings.json
*.key
*.pem

# Certificaten met een private sleutel erin. Een Cloudflare Origin Certificate hoort
# NOOIT in deze repo: site/ wordt met robocopy /MIR naar de webroot gespiegeld, en
# deploy/bootstrap.ps1 neemt daarom alleen een PAD naar een .pfx aan die ergens anders
# staat (C:\certs\ bijvoorbeeld). Deze regels zijn de tweede lijn, voor het geval
# iemand er toch een neerzet.
*.pfx
*.p12
*.pkcs12
*.jks

# Logs en tijdelijke bestanden
*.log
sniff.log
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
site/_var-*.html
site/_shot.html

# Gebouwde hulpprogrammas -- bouw ze zelf met build.cmd
tools/pfx-maker/PfxMaker.exe

# Ruwe momentopname van een lopende game. Bevat de Riot ID's van je medespelers.
live-sample.json

# Build orders uit eigen games, nog niet gedeeld.
buildorders.jsonl

# Per-minute curves fetched from match history for games that were opened.
#
# Already covered by /data/ and *.jsonl, and named here for the same reason
# buildorders.jsonl is: it carries a minute-by-minute series for all ten seats of
# other people's games, which is the behavioural fingerprint shared/types.ts
# warns about under Verloop. It stays on the machine that fetched it. The
# uploader is built against MatchStore alone and never opens this.
historie-tijdlijnen.jsonl

# The site's own images are tracked, all of them, and that is deliberate.
#
# These four and the champion portraits used to be ignored on the reasoning that
# they are generated and can be regenerated. They cannot, not where it matters:
# deploy/publish.ps1 hard-resets the server checkout to origin/main and then runs
# only refresh.mjs and build.mjs. Nothing there fetches a portrait or draws an
# atmosphere image, so anything not in git simply does not exist on the web
# server -- while build.mjs keeps writing references to it. That was 220 broken
# portraits across the champion pages, leeg.png missing on 112 of them, and the
# hero without its background.
#
# 5.4 MB against a repository that already carries 4.3 MB of icons, splashes,
# items and spells for exactly this reason.
